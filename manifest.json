{
  "manifest_version": 3,
  "name": "Dropdown Values Copier",
  "version": "1.0.0",
  "description": "Copy all values from a dropdown or combobox to clipboard via right-click context menu.",
  "permissions": ["contextMenus", "scripting", "clipboardWrite"],
  "host_permissions": ["<all_urls>"],
  "background": {
    "service_worker": "background.js"
  }
}
